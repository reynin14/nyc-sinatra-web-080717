<html>
<h1>Edit a figure!</h1>
<form method="POST" action="/figures/<%= @figure.id %>">
  Figure Name: <input type="text" id="figure_name" name="figure[name]" value="<%= @figure.name %>"></input>
  <br><br>
  <u>Choose a Title:</u>
  <br><% @titles.each do |title| %>
  <br><%= title.name %> <input type="checkbox" name="figure[title_ids][]" id="<%= title.id%>" value="<%=title.id%>" <%='checked' if @figure.titles.include?(title)%>></input>
  <% end %>
  <br>
  Or create a new one!
  <input type="text" id="new_title" name="title[name]"></input>
  <br><br>
  <u>Choose a Landmark:</u>
  <br><% @landmarks.each do |landmark| %>
  <%= landmark.name %> <input type="checkbox" id="landmark_<%=landmark.id %>" name="figure[landmark_ids][]" value=" <%= landmark.id %>" <%='checked' if @figure.landmarks.include?(landmark)%></input>
  <br><% end %>
  <br>
  Or create a new one!
  <br><input type="text" id="new_landmark" name="landmark[name]"></input>
  <br><br>
  <input type="submit" value="Edit Figure">
</form>
</html>
